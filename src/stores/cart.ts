import{defineStore}from'pinia';import{api}from'../api';import type{Cart,Product}from'../types';import{useSessionStore}from'./session'
export const useCartStore=defineStore('cart',{state:()=>({cart:{items:[],productAmount:0,totalQuantity:0,deliveryThreshold:10}as Cart,loading:false}),actions:{async load(){await useSessionStore().ensureLogin();this.loading=true;try{this.cart=await api.cart()}finally{this.loading=false}},async set(product:Product,quantity:number){const map=new Map(this.cart.items.map(i=>[i.product.id,i.quantity]));map.set(product.id,Math.max(0,quantity));this.cart=await api.updateCart([...map].map(([productId,quantity])=>({productId,quantity})))},quantity(id:string){return this.cart.items.find(i=>i.product.id===id)?.quantity??0}}})
