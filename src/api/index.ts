import {request} from './request';import type{Address,Cart,Category,Order,Product}from'../types'
interface PageResult<T>{items:T[];total:number;page:number;pageSize:number}
export const api={login:()=>request<{token:string;user:Record<string,string>}>('/auth/mock-login',{method:'POST',data:{code:'mock'}}),home:()=>request<{campus:{name:string};banners:Array<{id:string;title:string;subtitle:string;badge:string;color:string}>;categories:Category[];hotProducts:Product[]}>('/home'),products:async(categoryId='all',keyword='')=>(await request<PageResult<Product>>(`/products?categoryId=${categoryId}&keyword=${encodeURIComponent(keyword)}`)).items,product:(id:string)=>request<Product>(`/products/${id}`),cart:()=>request<Cart>('/cart'),updateCart:(items:Array<{productId:string;quantity:number}>)=>request<Cart>('/cart',{method:'PUT',data:{items}}),addresses:()=>request<Address[]>('/addresses'),coupons:()=>request<any[]>('/coupons'),slots:()=>request<Array<{id:string;label:string;available:boolean}>>('/delivery/slots'),checkout:(data:Record<string,unknown>)=>request<any>('/orders/checkout',{method:'POST',data}),createOrder:(data:Record<string,unknown>)=>request<Order>('/orders',{method:'POST',data}),payOrder:(id:string)=>request<Order>(`/orders/${id}/pay`,{method:'POST'}),orders:async(status='all')=>(await request<PageResult<Order>>(`/orders?status=${status}`)).items,order:(id:string)=>request<Order>(`/orders/${id}`),cancelOrder:(id:string)=>request<Order>(`/orders/${id}/cancel`,{method:'POST'}),advanceOrder:(id:string)=>request<Order>(`/orders/${id}/mock-advance`,{method:'POST'})}
