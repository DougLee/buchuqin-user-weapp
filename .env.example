# 开发环境 API 地址配置模板
# 用法：复制本文件为 .env.development，取消注释要用的场景（二选一），重启 pnpm dev:mp-weixin 生效。
# 不创建 .env.development 时，开发默认连 http://localhost:3100/api/v1（本机 API，src/api/request.ts 内置）。
# 生产地址由 .env.production 管，与本文件无关。

# 场景A：开发直连生产 API（不用起本地后端；需生产已部署微信登录凭证，否则登录 501）
# VITE_API_BASE_URL=https://buchuqin.com/api/v1

# 场景B：真机预览连开发机（手机与电脑同一 WiFi；Mac 局域网 IP 用 ifconfig 查；
#         手机上开发版右上角菜单 → 打开调试，跳过域名校验）
# VITE_API_BASE_URL=http://192.168.x.x:3100/api/v1
